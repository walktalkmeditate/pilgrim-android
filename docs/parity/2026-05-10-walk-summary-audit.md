# Parity Spec: WalkSummary

| field | value |
|---|---|
| **iOS pin** | `v1.5.0` = `db4196e` |
| **Android HEAD** | `01e7824` |
| **Generated** | 2026-05-10 |
| **Type** | audit |
| **Generator** | ios-parity skill |

---

## iOS source map

- `Pilgrim/Scenes/WalkSummary/ActivityInsightsView.swift` — 67 LOC — Talk-pct + meditation-count insight rows under a parchment card heading.
- `Pilgrim/Scenes/WalkSummary/ActivityListView.swift` — 106 LOC — Merged talks + meditation intervals as a chronologically sorted activity list with avatar circles.
- `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift` — 263 LOC — Stacked timeline with meditation/talking segments, tap-to-select tooltip, embedded pace sparkline.
- `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift` — 135 LOC — `AVAudioPlayer` wrapper with toggle/cycleSpeed state machine + `AudioSessionCoordinator` lifecycle.
- `Pilgrim/Scenes/WalkSummary/ElevationProfileView.swift` — 102 LOC — Stone-tinted altitude path with vertical gradient fill + min/max chip line.
- `Pilgrim/Scenes/WalkSummary/FaviconSelectorView.swift` — 50 LOC — 44pt circular flame/leaf/star toggles with tap-to-deselect behavior.
- `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift` — 101 LOC — 50-bucket downsampled speed line with stone gradient under-fill.
- `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift` — 218 LOC — Horizontal LazyHStack with viewAligned snap, long-press activation, scaleEffect spring.
- `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift` — 146 LOC — Full-screen black sheet with drag-to-dismiss + Pin / Open in Photos chips.
- `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift` — 312 LOC — 4-state gate (toggle/permission/loading/empty) with 300ms deferred skeleton + scenePhase-safe re-fetch.
- `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift` — 247 LOC — Per-recording playable row with edit-mode TextEditor, copy chip, expand-280-or-7-lines clamp.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift` — 974 LOC — The master `NavigationStack > ScrollView > VStack` with ~17 sections + reveal sequence + onAppear cascade.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift` — 87 LOC — Extension owning `combinedAnnotations`, photo-pin tap routing, map mask, polyline fallback.

## Android source map

- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryCalloutProse.kt` — 85 LOC — Pure-fn priority chain (Seasonal → LongestMed → LongestWalk → TotalDistance) with `THRESHOLDS` ladder.
- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryDetailsCard.kt` — 53 LOC — Caller-gated paused-row card on `parchmentSecondary` with `pilgrimType.body` label/value.
- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryRevealAnimations.kt` — 71 LOC — `RevealPhase` enum + `targetAlpha` + `rememberRevealAlpha` + duration/delay constants table.
- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryTopBar.kt` — 113 LOC — Custom 64dp `Box` top bar with title centered + Done at `CenterEnd` + Locale.ENGLISH long-date.
- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt` — 848 LOC — Compose root: 20 numbered sections, reveal LaunchedEffect, count-up animation, share/etegami/prompts plumbing.
- `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt` — 1552 LOC — Hilt VM with `WalkSummary` aggregate, `WalkSummaryUiState` sealed class, prompts coordinator, photo pin/unpin, etegami share, seal-revealed gate.

---

## Drift table — per lens

### Lens: behavior

| iOS finding | Android finding | severity | claim |
|---|---|---|---|
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:451-460@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryCalloutProse.kt:40-73@01e7824` | matches | Milestone priority chain: seasonal marker → longest meditation (strict-over-nonzero) → longest walk (strict-over-nonzero) → cumulative distance crossing → null. Both sides use `> 0` nonzero gates. iOS quote: `"for m in milestones where totalUnits >= m && pastUnits < m"`. Android quote: `"if (totalUnits >= m && pastUnits < m)"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:451-460@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryCalloutProse.kt:38@01e7824` | matches | TotalDistance milestone ladder is the same 7-tier list `[10, 25, 50, 100, 250, 500, 1000]` on both sides. iOS: `"let milestones = [10, 25, 50, 100, 250, 500, 1000]"`. Android: `"private val THRESHOLDS = listOf(10, 25, 50, 100, 250, 500, 1000)"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:46-48@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:152-177@01e7824` | matches | Reveal phase machine has three vertices: Hidden / Zoomed / Revealed. iOS quote: `"enum RevealPhase { case hidden, zoomed, revealed }"`. Android implements identical sealed enum + 800ms `ZOOM_HOLD_MS` delay. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:46-48@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:167-177@01e7824` | drift-cosmetic | Reveal-skip gate diverges. iOS skips reveal sequence only when route coords are empty; Android additionally skips on `reduceMotion` (Settings.Global ANIMATOR_DURATION_SCALE == 0f). Android quote: `"if (s.summary.routePoints.isEmpty() || reduceMotion) { revealPhase = RevealPhase.Revealed; return@LaunchedEffect }"`. iOS handles reduce-motion per-section via `withAnimation` rather than at the phase machine; Android hoists it. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:352-374@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:178-191@01e7824` | drift-critical | Distance count-up animation curve mismatch. iOS uses an explicit smoothstep (`p*p*(3-2*p)`) discretized into 30 ticks across 2.0s with a per-tick generation guard. Android uses Compose `animateFloatAsState` with a custom `SmoothStepEasing` over `COUNT_UP_DURATION_MS` — frame-driven, not 30 fixed steps. iOS quote: `"let eased = progress * progress * (3 - 2 * progress); animatedDistance = target * eased"`. Functionally close but the discretization granularity differs; needs visual confirmation that the easing curve matches. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:352-374@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:174-176@01e7824` | drift-critical | Reveal cinematic mismatch. iOS sequence: cameraZoom 16, cameraDuration 0.1s → wait 0.8s → cameraDuration 2.5s + bounds + opacity 0.6s easeInOut + start count-up. Android: just `delay(ZOOM_HOLD_MS = 800ms)` between Zoomed and Revealed; the 2.5s camera ease and 0.1s initial zoom-in are not represented in the Screen-level reveal. iOS quote: `"cameraDuration = 0.1; revealPhase = .zoomed; DispatchQueue.main.asyncAfter(deadline: .now() + 0.8) { cameraDuration = 2.5; ... withAnimation(.easeInOut(duration: 0.6)) { revealPhase = .revealed } }"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:319-543@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryRevealAnimations.kt:23-29@01e7824` | matches | Per-section reveal stagger duration / delay constants align with iOS. iOS: `".animation(.easeIn(duration: 0.6).delay(0.2), value: revealPhase)"` etc. Android: `"REVEAL_DURATION_DEFAULT_MS = 600 / REVEAL_DURATION_HERO_MS = 600 / REVEAL_DURATION_QUOTE_MS = 800 / REVEAL_DURATION_CALLOUT_MS = 800 / REVEAL_DELAY_STATS_MS = 200 / REVEAL_DELAY_CELESTIAL_MS = 300 / REVEAL_DELAY_BREAKDOWN_MS = 400"`. Note shared `CELESTIAL_MS` constant powers BOTH the celestial line AND the milestone callout — semantic coupling that drifts if iOS retunes one separately. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:16-26@db4196e` | — | missing | iOS `AudioPlayerModel.toggle(relativePath:)` has 3 branches (resume / pause / play-fresh) keyed on `currentPath==relativePath`, `isPlaying`, and `player != nil`. Android `WalkSummaryViewModel.kt:1071` only `playback.stop()` is referenced; the equivalent state machine lives in `VoicePlaybackController` which is OUT of the audited file set. iOS quote: `"if currentPath == relativePath { if isPlaying { pause() } else if player != nil { resume() } } else { play(relativePath: relativePath) }"`. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:11@db4196e` | — | missing | iOS playback speed cycle is a fixed `[1.0, 1.5, 2.0]` array with `firstIndex(of:)` + modulo wrap and reset-to-1.0 on miss. No equivalent surfaced in the audited Android files. iOS quote: `"private static let speeds: [Float] = [1.0, 1.5, 2.0]"`. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:110-115@db4196e` | — | missing | iOS 0.1s (10 Hz) `Timer.scheduledTimer` drives playback progress on the main run loop — magic 100ms tick. No equivalent surfaced in the audited Android files (the playback controller is out-of-slice). iOS quote: `"progressTimer = Timer.scheduledTimer(withTimeInterval: 0.1, repeats: true) { [weak self] _ in self?.updateProgress() }"`. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:129-134@db4196e` | — | missing | iOS `audioPlayerDidFinishPlaying` marshals to main via `DispatchQueue.main.async` before tearing down player + releasing audio session. The required Android STATE_ENDED parity lives in the out-of-slice playback controller. iOS quote: `"DispatchQueue.main.async { [weak self] in self?.stopPlayer(); AudioSessionCoordinator.shared.deactivate(consumer: \"audioPlayer\") }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:42-50@db4196e` | — | missing | iOS carousel clears the long-press activation the moment a horizontal scroll begins (`.interacting` phase), not at end. Android equivalent (per `WalkSummaryScreen.kt:480-495`) handles segment selection but no carousel scroll-phase observer surfaces in the audited files. iOS quote: `"if newPhase == .interacting && activeID != nil { activeID = nil }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:126-128@db4196e` | — | missing | iOS long-press `minimumDuration: 0.4` (400ms) — Android default `combinedClickable` long-press uses `ViewConfiguration.longPressTimeout` (~500ms) and there's no override in the audited slice. iOS quote: `".onLongPressGesture(minimumDuration: 0.4) { onLongPress() }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:76-87@db4196e` | — | missing | iOS one-shot `hasCommitted` latch on the pin button gates `onCommit` to fire at most once per sheet lifecycle (defends against fullScreenCover dismiss-animation hit-test race). No equivalent surfaced in audited files; PhotoPreviewSheet equivalent likely exists in the photo-reliquary slice (out of audit scope). iOS quote: `"guard !hasCommitted else { return }; hasCommitted = true; UIImpactFeedbackGenerator(style: .soft).impactOccurred(); onCommit(); onDismiss()"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:63-72@db4196e` | — | missing | iOS drag-to-dismiss threshold 120pt + spring(response: 0.3, damping: 0.8) snap-back. Android slice does not own the equivalent sheet. iOS quote: `"if value.translation.height > 120 { UIImpactFeedbackGenerator(style: .light).impactOccurred(); onDismiss() }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:58-77@db4196e` | — | missing | iOS 4-state reliquary gate: `isToggleOn × isPermissionGranted × candidates.isEmpty × showLoadingSkeleton`. Android equivalent is the Stage 7-A `PhotoReliquarySection` which is OUT of the audited Android files. iOS quote: `"if isToggleOn { if isPermissionGranted { if !candidates.isEmpty { content } else if showLoadingSkeleton { loadingSkeleton } } else { permissionRevokedPrompt } }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:80-91@db4196e` | — | missing | iOS deferred-skeleton 300ms `DispatchQueue.main.asyncAfter` with double-check-after-delay to skip skeleton on fast paths. Out of audited Android slice. iOS quote: `"DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) { if self.candidates.isEmpty && self.shouldRender { self.showLoadingSkeleton = true } }"`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:101-124@db4196e` | — | missing | iOS scenePhase==.active observer re-fetches candidates only on (gate open AND empty) OR clears on (gate closed AND non-empty); explicitly avoids wiping in-memory pin state on every foreground. Out of audited Android slice (likely lives in `PhotoReliquarySection.kt`). |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:259-279@db4196e` | — | missing | iOS `fetchGeneration: UInt &+= 1` monotonic counter so only the most-recent fetch's completion handler writes candidates. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:284-311@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:611-720@01e7824` | drift-critical | Pin/unpin semantics differ. iOS `commit()` optimistically rewrites the local candidates entry BEFORE calling `DataManager.pin/unpinPhoto` — single-photo API, no URI grant lifecycle. Android `pinPhotos(uris: List<Uri>)` runs on `persistenceScope`, takes persistable URI grants, returns `PinPhotosResult(insertedIds, droppedOrphanUris)`, and releases grants on orphans. Android quote: `"persistenceScope.launch { ... runCatching { context.contentResolver.takePersistableUriPermission(uri, Intent.FLAG_GRANT_READ_URI_PERMISSION) }"`. Android adds the URI-grant lifecycle that iOS PhotoKit doesn't need; this is structural drift, not a bug. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:85-94@db4196e` | — | missing | iOS defers seek by 100ms after togglePlay when starting fresh-then-seeking from inactive state — Archetype A 100ms anchor. Out of audited Android files (lives in row composable, not in VM). iOS quote: `"onTogglePlay(); DispatchQueue.main.asyncAfter(deadline: .now() + 0.1) { onSeek(fraction) }"`. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:27-36@db4196e` | — | missing | iOS transcription expand threshold = `count > 280 OR newline-count > 7`. Out of audited Android files. iOS quote: `"text.count > transcriptionExpandThreshold || text.split(separator: \"\\n\", omittingEmptySubsequences: false).count > 7"`. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:139-154@db4196e` | — | missing | iOS Done button trims whitespace+newlines and skips persistence on empty; always exits edit mode regardless of save success. Out of audited Android files. iOS quote: `"let trimmed = editText.trimmingCharacters(in: .whitespacesAndNewlines); if !trimmed.isEmpty { onTranscriptionSave?(trimmed) }; isEditing = false"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:259-269@db4196e` | — | missing | iOS auto-transcription poll loop: 30 iterations × 2-second sleep (60s budget) with cooperative cancel checks, gated by `UserPreferences.autoTranscribe`. No equivalent surfaced in audited Android files; Android observes the transcription Flow rather than polling (per Stage 5-G memory). iOS quote: `"for _ in 0..<30 { try? await Task.sleep(for: .seconds(2)); guard !Task.isCancelled else { return }; await MainActor.run { reloadTranscriptionsFromDatabase() }; if !transcriptions.isEmpty { return } }"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:141-145@db4196e` | — | missing | iOS observer triggers DB reload when `transcriptionService.state == .completed`. Android equivalent reactivity lives via the Room hot Flow (`observeVoiceRecordings(walkId)`) per VM lines 508-541, which is the Compose-Flow equivalent rather than an explicit completion-state observer. Different shape; flag for human. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:117-136@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:134@01e7824` | drift-cosmetic | iOS `onAppear` runs 6+ side effects in fixed order (load transcriptions, compute snippets, compute milestone, conditional celestial, start reveal, polling, light reading, restore shared-state, load reliquary). Android collapses this to `LaunchedEffect(Unit) { viewModel.runStartupSweep() }` + the `state` flow's eager-collected `buildState()`. Same outcomes but very different sequencing — onAppear's intentional reliquary-LAST ordering may not be replicated. iOS comment: `"// Reliquary fetch is intentionally LAST so the parent owns the Photos result write"`. |
| `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift:127-144@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:480-495@01e7824` | matches | Activity timeline tap-to-toggle segment selection with deselect-via-empty-area. Android wires `onSegmentSelected → zoomTargetBounds` and `onSegmentDeselected → null` symmetrically. iOS uses `withAnimation(.easeInOut(duration: 0.2))` per side-effect; Android uses Compose's animateFloatAsState defaults around the bounds change. |
| `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift:21-33@db4196e` | — | missing | iOS z-orders 'meditating' segments behind 'talking' AND uses different bar heights (16pt for meditating, 10pt for talking). Out of audited Android slice. iOS quote: `"ForEach(segments.filter { $0.type == .meditating }) { segment in segmentRect(segment, totalWidth: totalWidth, height: 16) }; ForEach(segments.filter { $0.type == .talking }) { segment in segmentRect(segment, totalWidth: totalWidth, height: 10) }"`. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:53-57@db4196e` | — | missing | iOS PaceSparkline filters samples to `speed > 0.3 m/s` before bucketing to discard low-velocity dwell. Out of audited Android slice. iOS quote: `"let samples = routeData.filter { $0.speed > 0.3 }"`. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:59-69@db4196e` | — | missing | iOS sparkline downsamples at `step = max(1, samples.count / 50)` — caps render at ~50 points. Out of audited Android slice. iOS quote: `"let step = max(1, samples.count / 50)"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:137-140@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1071-1080@01e7824` | matches | Lifecycle teardown: iOS `onDisappear { pollingTask?.cancel(); pollingTask = nil }`. Android `onCleared { playback.stop() }` (NOT release — Singleton outlives VM, Stage 2-D pattern). Different scope but both perform explicit teardown of long-running async work. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:73-77@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:789-803@01e7824` | matches | FaviconSelector `onChange` writes via `DataManager.setFavicon` synchronously on selection change; tap-same deselects. Android `setFavicon` does optimistic StateFlow flip + `persistenceScope.launch { repository.setFavicon }` + `compareAndSet` revert on failure; tap-same → null. Same UX semantics, Android adds defensive revert. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:204-210@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1309-1312@01e7824` | matches | Past walks fetch limit. iOS uses two limits: 20 (recent snippets) and 100 (milestone). Android `MILESTONE_CALLOUT_PAST_WALKS_LIMIT = 100` matches iOS milestone fetchLimit. iOS snippet-side fetch (limit 20) is not directly mirrored in audited Android files — light-reading snippet generation may live elsewhere. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:421-461@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1365-1385@01e7824` | drift-critical | Milestone history-aware computation differs in shape. iOS reads up to 100 prior walks once and uses pre-computed Walk fields (`walk.distance`, `walk.meditateDuration`). Android `detectMilestoneFor` reads ALL walks (`repository.allWalks()`) then for EACH past walk re-reads `repository.locationSamplesFor(walk.id)` and re-haversines — N+1 query pattern. Android quote: `"val finished = repository.allWalks().filter { it.endTimestamp != null }; ... walkDistanceMeters(repository.locationSamplesFor(walk.id).map { ... })"`. Behavioral parity probable but perf diverges sharply on power users with thousands of walks. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:816-826@db4196e` | — | missing | iOS `markSharedAndReveal` honors Reduce Motion: instant true vs animated 1.2s easeInOut. The Android share/light-reading reveal is gated via `lightReadingDisplay` flow — no comparable per-action animated reveal surfaced in audited files. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:29-46@db4196e` | — | missing | iOS `combinedAnnotations` is computed (not stored) — re-derives every body eval based on toggle + permission + photoCandidates.isPinned. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:13-23@db4196e` | — | missing | iOS photo annotation tap routes to carousel (sets `activePhotoID` with 0.2s easeInOut), explicitly NOT the preview sheet. Out of audited Android slice. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:101-105@01e7824` | extra | Android `WalkSummaryUiState` sealed class with explicit Loading / Loaded / NotFound. iOS WalkSummaryView likely uses a non-sealed pattern (optional binding on `walk`) — the `NotFound` case is an Android explicit choice. Quote: `"sealed class WalkSummaryUiState { data object Loading; data class Loaded(val summary); data object NotFound }"`. Worth verifying iOS does NOT have a parallel three-state load model. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:380-386@01e7824` | extra | Android `state` is `flow { emit(buildState()) }.stateIn(viewModelScope, Eagerly, Loading)` — runs `buildState()` synchronously during VM construction. iOS does the equivalent via `onAppear`-time computeRecentWalkSnippets/computeMilestone. Different lifecycle binding. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1093-1099@01e7824` | extra | Android per-walk-once seal-reveal gate via `SealRevealStore` is a USER-REQUESTED divergence (iOS replays seal reveal on every entry). Comment: `"Per-walk-once seal-reveal gate: skip the reveal if this walk's uuid is already in the SealRevealStore. User-requested divergence from iOS (which replays)."`. Intentional — do NOT regenerate. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1196-1231@01e7824` | extra | Android explicitly hops route segments + walk annotations classification to `Dispatchers.Default` because `buildState()` runs on viewModelScope's Main dispatcher. iOS doesn't have the equivalent Main-default-on-launch concern. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:821-884@01e7824` | extra | Android `openPromptsSheet` has TWO re-entry guards (cache hit + `promptsBuildJob.isActive`) preventing double geocoder rate-limit + ML Kit cost. iOS sheet presentation has its own throttle but no equivalent in audited files. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1437-1474@01e7824` | extra | Android etegami share runs on `Dispatchers.Default` with `etegamiShareMutex.tryLock()` non-blocking guard + `bitmap.recycle()` finally. iOS doesn't have an etegami concept here. Stage 2-E pattern says IO is canonical for file writes — flag for `Default` vs `IO` correctness review. |

### Lens: ui-visual

| iOS finding | Android finding | severity | claim |
|---|---|---|---|
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:53-92@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:285-602@01e7824` | drift-cosmetic | Section ordering. iOS lists ~17 sections in fixed VStack order: map, reliquary, intention, elevation, journeyQuote, durationHero, milestone?, statsRow, weather, celestial, timeBreakdown, FaviconSelector, activityTimelineBar, activityInsights, activityList, recordings?, prompts, details, lightReadingCard?, shareCard. Android numbered list places Etegami + ShareJourney inside `routePoints.size >= 2` guard at section 20 while LightReading is OUTSIDE it (section 19). iOS WalkSharingButtons gate matches but Light Reading position relative to share gate may differ; verify against `WalkSummaryView.swift:86-89` ordering. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:96-105@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryTopBar.kt:55-96@01e7824` | matches | Top bar layout. iOS uses NavigationStack toolbar with forced `.toolbarBackground(Color.parchment, for: .navigationBar)` + `.toolbarBackground(.visible, for: .navigationBar)` to plug the scroll-bleed seam. Android uses `Box(fillMaxWidth)` with title centered via `Alignment.Center`, Done at `Alignment.CenterEnd`, opaque parchment background. Equivalent intent — Android comment explicitly justifies Box-not-Row: `"a Row + weight(1f) trick centers within only (width - DoneWidth) and the title drifts visibly left of true center"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:106-115@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryTopBar.kt:77-93@01e7824` | matches | Top bar typography + tints. Both use Cormorant SemiBold-17 for title on `.ink`/`pilgrimColors.ink`, and Done in `.stone`/`pilgrimColors.stone`. iOS quote: `"Text(dateTitle).font(Constants.Typography.heading).foregroundColor(.ink); Button(\"Done\") { dismiss() }.foregroundColor(.stone)"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:65-74@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:725-743@01e7824` | matches | Map mask gradient. iOS: 320pt height, RadialGradient(white→white→white-0%) with startRadius 80 / endRadius 180, `.padding(.horizontal, -Constants.UI.Padding.normal)` for full-bleed. Android: 320dp height, `.graphicsLayer { compositingStrategy = CompositingStrategy.Offscreen }` + `Brush.radialGradient(0f→white, 0.45f→white, 1f→transparent)` with `BlendMode.DstIn`. Comment: `"0.45 stop matches iOS's 80/180 startRadius/endRadius ratio"`. The 80/180 = 0.444…; Android rounds to 0.45 (1.3% drift, not visually meaningful). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:75-84@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:778-786@01e7824` | drift-cosmetic | Empty-route fallback height. iOS uses 280pt for the placeholder while live map is 320pt — DIFFERENT heights, intentional layout shift on resolve. Android uses 320.dp for both placeholder and live map (intentional, comment: `"Two literals must stay in sync"`). Android avoids the iOS layout jump. iOS quote: `".fill(Color.parchmentSecondary).frame(height: 280)"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:63@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:271-280@01e7824` | matches | Per-activity polyline + annotation pin colors. iOS: `walkingColor: walkTurning?.uiColor ?? .moss`. Android: walking=moss / talking=rust / meditating=dawn for polyline + startEnd=stone / meditation=dawn / voice=rust for pins. Token mapping aligned. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:332-348@db4196e` | — | missing | iOS milestone callout uses sparkles in `.dawn`, ink text, bg = `Color.dawn.opacity(0.1)`. Android `MilestoneCalloutRow` is referenced but its visual styling is OUT of the audited file set. Note: 0.1 opacity is NOT a Constants token (closest is `Constants.UI.Opacity.light = 0.12`) — magic literal. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:296-311@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:305-309@01e7824` | drift-cosmetic | Intention card render gate. iOS: `if let intention = walk.intention, !intention.isEmpty`. Android: `if (!intention.isNullOrBlank())`. `isEmpty` (length-zero) vs `isBlank` (whitespace-only) is a subtle predicate difference — a single-space intention renders on iOS but hides on Android. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:535-543@db4196e` | — | missing | iOS time-breakdown is `LazyVGrid` with 3 flexible columns + spacing `Padding.normal=16`. Audited Android files don't show the timeBreakdown composable directly; lives in `WalkStatsRow` or peer (out-of-slice). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:463-476@db4196e` | — | missing | iOS statsRow conditional: Distance always; Steps if `steps > 0`; Elevation if `walk.ascend > 1` (note 1m, not 0). Audited Android `WalkStatsRow` call (`WalkSummaryScreen.kt:396-408`) consumes `distanceMeters`, `ascendMeters`, `units` — gating logic lives inside the row composable (out of slice). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:288-292@db4196e` | — | missing | iOS elevationProfile gates on `altitudes.count > 5 AND maxAlt - minAlt > 1` (filter noise + minimum data). No elevation profile composable surfaced in audited Android files. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:319-543@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:347-462@01e7824` | drift-critical | Reveal alpha implementation. iOS uses `.opacity(revealPhase == .hidden ? 0 : 1).animation(.easeIn(duration: 0.6/0.8).delay(...), value: revealPhase)` per section. Android uses `Modifier.alpha(rememberRevealAlpha(...))` value-form across ALL 7 sections. Android quote: `"modifier = Modifier.alpha(rememberRevealAlpha(...))"`. Per Stage 5-A memory: `Modifier.alpha(Float)` value-form re-runs in COMPOSITION phase causing many recomposes per frame; should be `Modifier.graphicsLayer { alpha = anim }` lambda form. iOS `.opacity` is render-only, not composition. Multiplied across 7 sections × 600-800ms reveal — perf cliff. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:319-543@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryRevealAnimations.kt:61-69@01e7824` | drift-cosmetic | Easing curve mismatch. iOS uses `.easeIn(duration: 0.6)` (also documented as `Constants.UI.Motion.gentle = 0.6`). Android uses `tween(durationMs, delayMillis = delayMs, easing = EaseIn)`. The `EaseIn` Compose easing is starts-slow-finishes-fast; iOS `.easeIn` is also starts-slow-finishes-fast — semantically aligned. However: iOS lens flagged that SwiftUI's "default" is `easeInOut`, so any future SwiftUI default-curve drift would diverge silently. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:86-89@db4196e` | — | missing | iOS lightReadingCard transition: `reduceMotion ? .identity : .opacity.combined(with: .scale(scale: 0.97))`. Audited Android `WalkLightReadingCard` callsite at `WalkSummaryScreen.kt:553-557` — visual transition spec lives inside the card composable (out of slice). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:86-89@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:553-602@01e7824` | drift-critical | LightReading vs Share gating divergence. iOS gates LightReading inside `if let reading = lightReading, hasRevealedLightReading` AFTER detailsSection AND inside the same `if !walk.voiceRecordings.isEmpty` ladder; Share is always rendered. Android places LightReading OUTSIDE the `routePoints.size >= 2` guard (section 19) but Etegami + Share INSIDE (section 20). Comment: `"LightReading is OUTSIDE the guard ... etegami/journey-share are INSIDE"`. Verify which side matches iOS WalkSharingButtons rule. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:36-37@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:108-237@01e7824` | drift-cosmetic | Compose stability cascade. iOS `@State var photoCandidates: [PhotoCandidate]` is fine for SwiftUI value-type diffing. Android `@Immutable data class WalkSummary` holds 14 list/sealed types including `Walk`, `List<RouteSegment>`, `List<WalkMapAnnotation>`, `List<VoiceRecording>`, `List<ActivityInterval>`, etc. Per Stage 4-D memory: `@Immutable` cascade is a LIE if any contained data class isn't itself stable. Android-specific perf concern; no iOS equivalent need. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:82-87@db4196e` | — | missing | iOS PhotoThumbnailView statics: size=88, cornerRadius=8, badgeInset=4, activeScale=1.05, centerIconSize=32, centerIconBackgroundSize=44. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:24-51@db4196e` | — | missing | iOS carousel: `ScrollView(.horizontal, showsIndicators: false) { LazyHStack(spacing: small) { ... }.scrollTargetLayout().padding(.horizontal, normal) }.scrollPosition(id: $activePhotoID, anchor: .center).scrollTargetBehavior(.viewAligned)`. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:202-225@db4196e` | — | missing | iOS skeleton shimmer animation: `.easeInOut(duration: 1.1).repeatForever(autoreverses: true)`, opacity 0.45↔0.85, 88×88 placeholder, 8pt corner. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift:21-33@db4196e` | — | missing | iOS timeline: 16pt-tall background, meditate=16pt, talk=10pt; 4pt corner. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityListView.swift:23-28@db4196e` | — | missing | iOS activity row icon avatar: 24×24 white-glyph circle with entry.color background. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityInsightsView.swift:10-29@db4196e` | — | missing | iOS insights card: VStack(.leading, spacing: small) padded normal, parchmentSecondary bg, normal corner radius. Token-aligned standard card. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/FaviconSelectorView.swift:32-40@db4196e` | — | missing | iOS favicon avatar: 44×44 circle (HIG min tap), unselected fill `Color.fog.opacity(0.15)` (0.15 is NOT a Constants token — Constants.UI.Opacity.light=0.12). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ElevationProfileView.swift:58-78@db4196e` | — | missing | iOS elevation fill = `LinearGradient(stone 0.3→stone 0.05, top→bottom)`; stroke = stone 0.6 at 1.5pt rounded. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:36-50@db4196e` | — | missing | iOS pace sparkline fill = stone 0.12→0.02; stroke = stone 0.45 at 1.5pt. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:88-115@db4196e` | — | missing | iOS preview-sheet buttons use `.regularMaterial` blur background + Capsule clip. No native Compose equivalent — needs Haze or solid-translucent. Out of audited Android slice. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryDetailsCard.kt:25-52@01e7824` | extra | Android `WalkSummaryDetailsCard` (Paused row) — Material3 Card on `parchmentSecondary`, label `pilgrimType.body` `.fog`, value `pilgrimType.body` `.ink`, gated caller-side on `pausedMillis > 0L`. iOS detailsSection is in `WalkSummaryView.swift:709-792` and uses different styling (token references, no separate composable). Worth verifying iOS lacks the explicit card. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:828-841@01e7824` | matches | Weather line composable — Icon 16dp, gap `PilgrimSpacing.xs (4dp)`, text `pilgrimType.caption`, tint `pilgrimColors.fog`. iOS doesn't have a documented "WalkSummaryWeatherLine" lens finding (likely lives in iOS WalkSummaryView at line ~330ish per the layout finding). Treating as matches by structural equivalence; verify lens coverage. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:613-639@01e7824` | extra | Android `SealRevealOverlay` ink resolution: base = `pilgrimColors.rust`, runtime tinted via `SeasonalColorEngine.applySeasonalShift` Intensity.Full at `walkDate` + `hemisphere`. iOS may pre-bake seasonal tint as asset. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:688-691@01e7824` | extra | Android `SnackbarHost` anchored to `Alignment.BottomCenter` of outer Box (intentional, avoids SealReveal collision). iOS likely uses inline banner / toast at top. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryDetailsCard.kt:1-53@01e7824` | extra | No `Modifier.semantics { ... }` or customActions across audited Android files (TopBar, DetailsCard, WeatherLine). iOS likely uses `.accessibilityElement` / `.accessibilityLabel` per Stage 6-B precedent. Accessibility regression flag. |

### Lens: data

| iOS finding | Android finding | severity | claim |
|---|---|---|---|
| `Pilgrim/Models/Walk/WalkPhotoMatcher.swift:6-15@db4196e` | — | missing | iOS `PhotoCandidate` (NOT persisted) carries localIdentifier, capturedAt, capturedLat/Lng + transient `isPinned` derived by WalkPhotoMatcher. Android persisted `WalkPhoto` is a Room entity with photoUri (content://) — different model shape. |
| `Pilgrim/Models/Walk/MapManagement/PilgrimAnnotation.swift:4-19@db4196e` | — | missing | iOS `PilgrimAnnotation` Identifiable with `id = UUID()`, CLLocationCoordinate2D, Kind enum (8 cases including `.photo(localIdentifier:)`). Android equivalent `WalkMapAnnotation` referenced in `WalkSummaryViewModel.kt:1196-1231` but specific case enumeration not in audited slice. |
| `Pilgrim/Models/Walk/MapManagement/RouteSegment.swift:3-13@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:135-138@01e7824` | matches | RouteSegment with coordinates polyline + activityType tag. Android keeps the same shape as a `List<RouteSegment>` field on `WalkSummary`. iOS uses stringly-typed `activityType` (literals walking/talking/meditating); Android shape not visible in audited slice but referenced. |
| `Pilgrim/Models/Walk/WalkFavicon.swift:3-22@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:789-803@01e7824` | matches | `WalkFavicon` enum cases flame/leaf/star with String rawValue persisted to Walk row. Android `WalkFavicon` referenced; raw values must round-trip exactly. iOS quote: `"enum WalkFavicon: String, CaseIterable { case flame, leaf, star }"`. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:46-48@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryRevealAnimations.kt:23-69@01e7824` | matches | Three-state `RevealPhase` enum (hidden/zoomed/revealed). Same on both sides. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:204-210@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1083@01e7824` | drift-critical | Past-walks read on Main. iOS `computeRecentWalkSnippets` calls `DataManager.dataStack.fetchAll` synchronously inside `onAppear` (Main thread, would ANR on Android). Android `buildState() → repository.getWalk(walkId)` runs through `flow { emit(buildState()) }.stateIn(viewModelScope, Eagerly)` — Eagerly emits via the collector which kicks off on Main. The suspend functions need to internally `withContext(IO)` or there's an Archetype A risk (validated for `runStartupSweep` at `WalkSummaryViewModel.kt:562-564`). Verify that `repository.getWalk` and the ~6 other reads at lines 1100-1194 internally hop to IO. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:430-435@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1309-1312@01e7824` | matches | Past walks fetch limit = 100 for milestone calc. Android `MILESTONE_CALLOUT_PAST_WALKS_LIMIT = 100` matches iOS `fetchLimit = 100`. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:300-307@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:665-672@01e7824` | drift-critical | Pin-photo write surface diverges. iOS single-photo `pinPhoto(to:, localIdentifier:, capturedAt:, capturedLat:, capturedLng:)` idempotent on `(walkID, localIdentifier)`. Android batch `pinPhotos(walkId, refs, pinnedAt, cap=MAX_PINS_PER_WALK)` returns `PinPhotosResult` with `droppedOrphanUris`. Different surface to handle URI grant lifecycle (no PhotoKit equivalent on Android). |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:308-310@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:744-769@01e7824` | drift-critical | Unpin diverges similarly. iOS `unpinPhoto(walkID:, localIdentifier:)` deletes Walk-Photo row; iOS does NOT release URI grant (PhotoKit-only). Android `unpinPhoto(photoId)` returns `UnpinPhotoResult.Removed(wasLastReference, photoUri)` — releases persistable URI grant ONLY when `wasLastReference == true`. Stage 7-A pattern, correct Android-only logic. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:629-633@db4196e` | — | missing | iOS `updateVoiceRecordingTranscription` writes via async CoreStore txn with optimistic local map mutation BEFORE persistence callback. Out of audited Android slice (lives in voice-row composable + repository). |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:42-48@db4196e` | — | missing | iOS resolves recording paths against Documents directory and bails on missing file. Android `voiceRecordings` are observed as Room entities (out of audited slice file); file resolution lives in playback controller. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:774-779@db4196e` | — | missing | iOS `isFileAvailable` does synchronous `FileManager.fileExists` on Main thread per Composable read — works on iOS, would trip StrictMode on Android. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:647-651@db4196e` | — | missing | iOS waveform decode uses `Task.detached(priority: .utility)` for the heavy DSP. Out of audited Android slice (lives in voice-row composable). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:667-674@db4196e` | — | missing | iOS `DataManager.deleteRecordingFile` invoked synchronously from Delete-confirmation alert button on Main. Archetype A. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:129-134@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1071-1080@01e7824` | matches | iOS `AudioSessionCoordinator.shared.deactivate(consumer: "audioPlayer")` on player-finished; Android Singleton `VoicePlaybackController` managed via VM `onCleared { playback.stop() }` (NOT release — Stage 2-D pattern). Lifecycle ownership differs but both perform clean teardown. |
| `Pilgrim/Models/LightReading/WalkSharingTracker.swift:12-37@db4196e` | — | missing | iOS `WalkSharingTracker` UserDefaults key `sharedWalkUUIDs` → `Array<String>` (Set semantics). Android equivalent should be DataStore Preferences `stringSetPreferencesKey("sharedWalkUUIDs")` — not visible in audited Android slice (lives in shared-row module). |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:134-136@db4196e` | — | missing | iOS reads `UserPreferences.walkReliquaryEnabled.value` per render — should be Flow-backed StateFlow on Android. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:121-124@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:411-478@01e7824` | matches | `UserPreferences.celestialAwarenessEnabled` + `UserPreferences.zodiacSystem`. Android `practicePreferences.celestialAwarenessEnabled` is a DataStore-backed Flow consumed by `lightReadingDisplay`, `celestialSnapshotDisplay`, `walkSummaryCalloutProseDisplay` (three flows, all combine with state). Toggle live on Android (combine emits); iOS reads `.value` per onAppear — Android is reactive-better. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:121-124@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1284-1289@01e7824` | drift-cosmetic | `zodiacSystem` read at buildState time. Android comment notes: `"Captured at build time; toggling zodiac system in Settings while summary is open does NOT re-derive (documented limitation)"`. iOS reads `UserPreferences.zodiacSystem.value` similarly only at onAppear — both have the same staleness window. Documented mismatch with `celestialAwarenessEnabled` (which IS reactive on Android). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:942-961@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:275@01e7824` | drift-cosmetic | `distanceMeasurementType` (.miles/.kilometers). Android exposes `distanceUnits: StateFlow<UnitSystem> = unitsPreferences.distanceUnits`, but `calloutInputs.units` is captured at buildState time. Android comment: `"toggling Metric ↔ Imperial mid-summary won't re-derive TotalDistance threshold prose"`. iOS reads `UserPreferences.distanceMeasurementType.safeValue` per format call (live). Behavior diverges on mid-summary toggle. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:126-128@db4196e` | — | missing | iOS `UserPreferences.autoTranscribe` gates polling loop. No equivalent autoTranscribe flag surfaced in audited Android slice; Android observes the transcription Flow directly per Stage 5-G memory. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:51-52@db4196e` | — | missing | iOS `AudioSessionCoordinator.shared` Singleton with consumer="audioPlayer", `.playbackOnly` mode. Android equivalent `VoicePlaybackController` Singleton out of audited slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:12@db4196e` | — | missing | iOS `TranscriptionService.shared` ObservableObject with `.state` enum. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:641-655@db4196e` | — | missing | iOS `WaveformCache.shared` actor with `markInFlight/samples/store/clearInFlight`. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:104-106@db4196e` | — | missing | iOS opens Apple Photos via `photos-redirect://` URL scheme. Android equivalent would be `ACTION_VIEW` on content URI — out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:240-243@db4196e` | — | missing | iOS opens Settings via `UIApplication.openSettingsURLString` to recover from revoked Photos permission. Android equivalent `Settings.ACTION_APPLICATION_DETAILS_SETTINGS` — out of audited Android slice. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:391-393@01e7824` | extra | Android `SealRevealStore.markRevealed(uuid)` / `isRevealed(uuid)` — per-walk seal-reveal gate persisted set. User-requested Android divergence; iOS replays. Endpoint: NEW persisted set, not in iOS. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:480-501@01e7824` | extra | Android `cachedShareFlow` via `flatMapLatest(state) → cachedShareStore.observe(walk.uuid)` with `SharingStarted.Eagerly`. iOS uses similar persisted cache but may not gate on Loaded. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1054-1069@01e7824` | extra | Android `readDateTaken(uri)` queries ContentResolver `MediaStore.MediaColumns.DATE_TAKEN` — Android-only (iOS PHAsset has `.creationDate` directly). |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1452-1485@01e7824` | extra | Android `shareEtegami` / `saveEtegamiToGallery` use `EtegamiBitmapRenderer.render` + `EtegamiPngWriter.writeToCache` + `EtegamiGallerySaver.saveToGallery` on `Dispatchers.Default`. iOS doesn't have an etegami feature in this slice. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:343-354@01e7824` | extra | Android `promptsCacheInvalidator` combines two Room observers (photos + transcribed recordings) to null the prompt cache on count change. iOS may rebuild prompts on every sheet open without a cache. |

### Lens: edge-cases

| iOS finding | Android finding | severity | claim |
|---|---|---|---|
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:451-460@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryCalloutProse.kt:62-63@01e7824` | matches | TotalDistance crossed-the-line invariant. iOS: `for m in milestones where totalUnits >= m && pastUnits < m`. Android: `if (totalUnits >= m && pastUnits < m)`. Identical boundary semantics. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:91-99@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryCalloutProse.kt:58@01e7824` | matches | Imperial mile factor `1609.344` and metric `1000.0` constants. Both sides use the same literals. iOS quote: `"let metersPerUnit = isMiles ? 1609.344 : 1000.0"`. Android quote: `"val unitFactor = if (inputs.units == UnitSystem.Imperial) 1_609.344 else 1_000.0"`. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:11@db4196e` | — | missing | iOS playback speed cycle `[1.0, 1.5, 2.0]` with `firstIndex(of:)` + modulo wrap. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:42@db4196e` | — | missing | iOS `FileManager.default.urls(for: .documentDirectory, in: .userDomainMask).first!` force-unwrap — recurs at WalkSummaryView.swift:647 + 777 (THREE call sites). Android `context.filesDir` is non-null; out of audited slice. |
| `Pilgrim/Scenes/WalkSummary/AudioPlayerModel.swift:110-115@db4196e` | — | missing | iOS 0.1s (10 Hz) progress timer with `[weak self]` capture. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:126-128@db4196e` | — | missing | iOS long-press 0.4s threshold. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:118-121@db4196e` | — | missing | iOS spring animation `response: 0.3, dampingFraction: 0.7` for active scale. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:63-72@db4196e` | — | missing | iOS drag-to-dismiss 120pt threshold + spring(0.3, 0.8) snap-back. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoPreviewSheet.swift:77-87@db4196e` | — | missing | iOS `hasCommitted` latch on pin button (defends double-fire-during-dismiss). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:36-41@db4196e` | — | missing | iOS deferred-skeleton 300ms threshold. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:25-29@db4196e` | — | missing | iOS `fetchGeneration: UInt &+= 1` monotonic counter pattern. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:101-124@db4196e` | — | missing | iOS scenePhase observer's careful re-fetch decision matrix. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:27-36@db4196e` | — | missing | iOS transcription expand `count > 280 OR newline-count > 7` dual trigger. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:158-162@db4196e` | — | missing | iOS lineLimit clamp = 7 lines (matches expand threshold). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:85-94@db4196e` | — | missing | iOS 0.1s seek-after-toggle delay (Archetype A anchor — without it seek silences first second). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:69-77@db4196e` | — | missing | iOS speed-pill swap at `> 1.0` (NOT `>=`) — playbackSpeed at exactly 1.0x renders inactive style. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:227-232@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:845-848@01e7824` | matches | Locale-pinned numeric formatting. iOS: `String(format: "%d:%02d", m, s)` (implicit C locale). Android: `String.format(Locale.US, "%.0f°%s", rounded, ...)`. Android explicit Locale.US per Stage 5-A/6-B memory. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:362-374@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:174-176@01e7824` | drift-critical | Reveal sequence delays. iOS: `cameraDuration = 0.1; revealPhase = .zoomed; asyncAfter(0.8) { cameraDuration = 2.5; ... withAnimation(.easeInOut(duration: 0.6)) { revealPhase = .revealed } }`. Android collapses to `revealPhase = Zoomed; delay(ZOOM_HOLD_MS = 800ms); revealPhase = Revealed` — the 0.1s initial cameraZoom and 2.5s pan-out animation are NOT mirrored at the Screen level (may live in `PilgrimMap`). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:376-392@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:178-191@01e7824` | drift-critical | Distance count-up. iOS 30 steps × 66ms with smoothstep `p*p*(3-2*p)` and per-tick generation guard. Android `animateFloatAsState(targetValue, tween(COUNT_UP_DURATION_MS, easing = SmoothStepEasing))` — frame-driven, not stepped. Visual fidelity should be close but exact curve match needs validation. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:204-215@db4196e` | — | missing | iOS recent-walks fetch limit 20 + `prefix(3)` for snippet preview. No equivalent surfaced in audited Android slice (likely lives in light-reading or snippet helpers). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:430-435@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1533@01e7824` | matches | Milestone fetch limit 100. Both sides match. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:286-292@db4196e` | — | missing | iOS elevation gates: `altitudes.count > 5 AND maxAlt - minAlt > 1m AND .frame(height: 48)`. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:466-471@db4196e` | — | missing | iOS asymmetric thresholds: steps > 0 (any step counts) but ascend > 1m (sub-meter hidden). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:319-543@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/summary/WalkSummaryRevealAnimations.kt:23-29@01e7824` | matches | Stagger delay constants 0.0/0.2/0.3/0.4s. Android uses `REVEAL_DELAY_STATS_MS = 200`, `REVEAL_DELAY_CELESTIAL_MS = 300`, `REVEAL_DELAY_BREAKDOWN_MS = 400` — same numbers. Note Android couples callout + celestial onto `CELESTIAL_MS = 300` constant, which is a documented coupling risk. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:259-268@db4196e` | — | missing | iOS auto-transcription poll loop 30 × 2s = 60s budget with `try? await Task.sleep` (Archetype I: try? swallows cancel sema). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView+Map.swift:65-78@db4196e` | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:725-786@01e7824` | drift-cosmetic | Map mask + heights. iOS map 320pt + radial 80/180; placeholder 280pt; negative `-Constants.UI.Padding.normal` horizontal padding for full-bleed. Android map 320dp + radial 0.45 stop; placeholder 320dp; no documented negative padding (full-bleed mechanism may differ). Heights 280 vs 320 placeholder is the cosmetic divergence. |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:925-931@db4196e` | — | missing | iOS bounds padding: 15% of range + 0.001 floor for single-point routes. No equivalent surfaced in audited Android slice (lives in map / `computeBoundsForTimeRange`). |
| `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:919-924@db4196e` | — | missing | iOS `?? 0` fallback on lats/lons min/max — produces Null Island bounds for empty arrays. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityInsightsView.swift:62-65@db4196e` | — | missing | iOS talk percentage `pct = Int((talkDuration / activeDuration) * 100)` truncation. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityListView.swift:91@db4196e` | — | missing | iOS sorts merged talks+meditations by ascending start date. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift:226-228@db4196e` | — | missing | iOS `totalDuration` floored at 1 second to avoid divide-by-zero. Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityTimelineBar.swift:240@db4196e` | — | missing | iOS `max(0, ...)` clamp for negative segment offsets (recordings before walk start). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:55@db4196e` | — | missing | iOS PaceSparkline 0.3 m/s threshold filter (recurs at line 83). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/PaceSparklineView.swift:74-79@db4196e` | — | missing | iOS sparkline 0.85 y-scale factor (leaves headroom). Out of audited Android slice. |
| `Pilgrim/Scenes/WalkSummary/ActivityInsightsView.swift:53-60@db4196e` | — | missing | iOS compact-duration switches at 60s boundary (sec → min). Out of audited Android slice (Android has `WalkFormat.duration` per WalkSummaryDetailsCard). |
| `Pilgrim/Scenes/WalkSummary/ElevationProfileView.swift:97-99@db4196e` | — | missing | iOS feet conversion factor 3.28084 (recurs at WalkSummaryView.swift:959). Android uses Imperial conversion 9/5+32 for temperature but no audited elevation conversion in slice. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1118-1127@01e7824` | extra | Android pace gate: `distanceKm >= 0.01 AND activeWalking >= 1_000L` (10m + 1s). iOS may use different thresholds. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1238@01e7824` | extra | Android `coerceAtLeast(0L)` on `activeMillis` masks underflow if totalPausedMillis exceeds totalElapsed. iOS may not coerce — divergent behavior under bad/replayed data. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1321-1323@01e7824` | extra | Android `?: 0.0` fallback on `pastWalksMaxDistance` after `maxOfOrNull`. Stage 4-D MEMORY: `maxByOrNull`/`maxOf` on equal-value collections silently tie-breaks; here `?: 0.0` could mask null distanceMeters → LongestWalk callout suppressed silently. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:651-662@01e7824` | extra | Android `runCatching { takePersistableUriPermission }` does NOT re-throw `CancellationException`. Same pattern repeats at 686-699, 754-766, 1054-1069. Stage 5-D memory: in suspending contexts this swallows CE. Audit each call site. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1452@01e7824` | extra | Android etegami `viewModelScope.launch(Dispatchers.Default)` does file I/O via `EtegamiPngWriter`. Stage 5-C memory says IO is canonical for file writes; using Default contends with whisper.cpp + may trip StrictMode. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:256-258@01e7824` | extra | Android `requireNotNull(savedStateHandle.get<Long>(ARG_WALK_ID))` throws on construction if nav arg missing. iOS would treat missing as "go to Home"; Android crashes the screen. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:368-378@01e7824` | extra | Android `_showSealReveal` field declaration order is load-bearing — `state`'s `flow { emit(buildState()) }` body fires DURING field-init in declaration order. Stage 2-C trap. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:116-237@01e7824` | extra | Android `WalkSummary` is `@Immutable` but holds 14 list/sealed-class field types. Compose stability walks transitively; `@Immutable` is a LIE if any contained type isn't itself stable. Stage 4-D/4-C/5-D MEMORY. None use `ImmutableList` (kotlinx-collections-immutable). Recompose perf risk. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:160-166@01e7824` | extra | Android `ANIMATOR_DURATION_SCALE == 0f` for reduce-motion detection. iOS uses `@Environment(\.accessibilityReduceMotion)` which is a binary user setting; Android compares float against 0f literally — a future system setting at e.g. 0.1f would silently leave full animations on. Behavioral divergence at the edge. |
| — | `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:643-652@01e7824` | extra | Android `PromptsSheetState.Loading` rendered as no-op (~0-2s blank gap on first open). Comment notes intentional. iOS sheet may show spinner. |

### Cross-cutting drift summary

| severity | count |
|---|---|
| drift-critical | 9 |
| drift-cosmetic | 11 |
| missing | 67 |
| extra | 23 |
| matches | 22 |

Worst offenders (drift-critical first):

- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:352-374@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:174-176@01e7824` — Reveal cinematic timing chain: iOS has 0.1s zoomed-in + 0.8s wait + 2.5s pan-out + 0.6s opacity ease; Android has only the 0.8s wait. The 2.5s camera pan and per-component cameraDuration handoff may live in `PilgrimMap` (out of slice) — verify.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:319-543@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:347-462@01e7824` — All 7 reveal sections use `Modifier.alpha(rememberRevealAlpha(...))` value-form on Android, which re-runs in COMPOSITION phase per Stage 5-A. iOS `.opacity()` is render-only. Should migrate to `Modifier.graphicsLayer { alpha = anim }` lambda form. Multiplied across 7 sections × 600-800ms reveal — measurable perf cliff.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:204-210@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1083@01e7824` — `buildState()` runs on viewModelScope's Main dispatcher (`SharingStarted.Eagerly`); Android only explicitly hops `runStartupSweep` and route classification to IO/Default. The ~6 repository reads at lines 1100-1194 must internally hop to IO or there's an Archetype A risk. Verify `WalkRepository` DAO discipline.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:421-461@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1365-1385@01e7824` — Android `detectMilestoneFor` runs N+1 Room queries: `repository.allWalks()` + per-past-walk `repository.locationSamplesFor(walk.id)` re-haversine. Power user with 10k walks pays 10k queries per Walk Summary open. iOS uses pre-computed `walk.distance` field directly. Behavior parity but perf parity broken.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:46-48@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:178-191@01e7824` — Distance count-up easing curve. iOS uses an explicit smoothstep `p*p*(3-2*p)` discretized into 30 ticks across 2.0s. Android uses `animateFloatAsState` with custom `SmoothStepEasing` over `COUNT_UP_DURATION_MS`. Curve shape probably matches but discretization differs.
- `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:300-307@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:665-672@01e7824` — Pin-photo write surface. iOS single-photo idempotent; Android batch with `PinPhotosResult(droppedOrphanUris)` + URI grant lifecycle. Structural drift driven by Android-only persistable URI grants. Correct for platform; flag for parity expectations.
- `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:308-310@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:744-769@01e7824` — Unpin URI grant lifecycle: Android releases ONLY when `wasLastReference == true` (Stage 7-A pattern). iOS no equivalent. Correct platform divergence.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:204-210@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1083@01e7824` (data-lens duplicate) — Same as the behavior-lens entry above; surfaced in two lenses.
- `Pilgrim/Scenes/WalkSummary/WalkSummaryView.swift:86-89@db4196e` ↔ `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryScreen.kt:553-602@01e7824` — LightReading vs Etegami/Share gating. Android places LightReading OUTSIDE `routePoints.size >= 2` guard while Etegami + Share are INSIDE. Verify which side matches iOS WalkSharingButtons + LightReadingCard ordering.

---

## Lens disagreements

Findings flagged by only one lens, surfaced for human triage:

- **`stability-cascade` at `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:108-237@01e7824`** — flagged by both Android ui-visual AND Android edge-cases (cross-lens consensus). iOS lens did not flag because Swift value types aren't subject to Compose stability inference. Why it matters: `@Immutable` on `WalkSummary` is a LIE if 14 contained list/sealed types aren't themselves stable; recompose perf degrades wholesale on every emit.
- **`async-delay` 0.1s seek-after-toggle at `Pilgrim/Scenes/WalkSummary/VoiceRecordingRow.swift:85-94@db4196e`** — flagged by iOS behavior + iOS edge-cases (consensus). Android voice-row composable not in audited slice; Android may have lost this pattern silently. ExoPlayer's `prepare()` is also async; without the deferred seek the just-started player no-ops the seek.
- **`async-delay` 300ms deferred skeleton at `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:80-91@db4196e`** — flagged by iOS behavior + iOS ui-visual + iOS edge-cases (3-lens consensus). Android `PhotoReliquarySection` not in audited slice; verify the 300ms delay + double-check pattern is replicated.
- **`leak-suspicion` `etegamiShareMutex` held across `bitmap.recycle()` at `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1467-1474@01e7824`** — Android-only, flagged by edge-cases. Documented intentional (cancellation between render and writer abandons bitmap to GC). Worth surfacing because GC delay could leak ~MB-sized bitmap until next collection. iOS lens cannot see this.
- **`builder-validation` `EtegamiShareIntentFactory.buildFromFile` at `app/src/main/java/org/walktalkmeditate/pilgrim/ui/walk/WalkSummaryViewModel.kt:1461-1466@01e7824`** — Android-only, flagged by edge-cases. Stage 2-F memory: `WorkRequest.build()` crashes ship through 6 review cycles when fakes wrap the boundary. Verify Robolectric test calls `EtegamiShareIntentFactory.buildFromFile` on the production class.
- **`magic-number` `1.05` active scale + `.scaleEffect(value)` at `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoCarouselView.swift:113-121@db4196e`** — flagged by iOS ui-visual + iOS edge-cases. Compose port must use `Modifier.graphicsLayer { val s = animatedScale.value; scaleX = s; scaleY = s }` not `Modifier.scale(animatedScale.value)` — Stage 5-A perf cliff. Out of audited Android slice; worth verifying when the carousel ports.
- **`scenePhase` re-fetch decision matrix at `Pilgrim/Scenes/WalkSummary/Reliquary/PhotoReliquarySection.swift:101-124@db4196e`** — flagged by iOS behavior + iOS edge-cases. iOS comment explicitly warns: re-fetching on every foreground wipes optimistic pin state. Android `Lifecycle.Event.ON_START`/`ON_RESUME` observer must replicate the careful gate-open-AND-empty matrix or reproduce the bug.

---

## Open questions

Ambiguities the lens readers couldn't resolve:

- The audited Android file set covers the **summary screen + VM + 3 small composables** (TopBar, DetailsCard, RevealAnimations, CalloutProse). Major iOS slice components — `AudioPlayerModel`, `ActivityTimelineBar`, `ActivityListView`, `ActivityInsightsView`, `ElevationProfileView`, `FaviconSelectorView`, `PaceSparklineView`, all three Reliquary files, `VoiceRecordingRow` — have NO audited Android peer in this report. The 67 "missing" findings are dominated by these unscanned Android peers. Re-run `/ios-parity audit WalkSummary` with a wider Android file set OR follow up with per-component audits (`/ios-parity audit ActivityTimelineBar`, etc.) to disambiguate "missing on Android" from "exists but not audited".
- Light Reading vs Etegami/Share gate ordering relative to `routePoints.size >= 2` — does iOS WalkSharingButtons gate match Android section 19 (LightReading) being OUTSIDE the >=2 guard while section 20 (Etegami + Share) is INSIDE? Verify against `WalkSummaryView.swift:86-92`.
- Reveal cinematic 2.5s camera pan-out — Android `WalkSummaryScreen.kt` only delays 800ms then transitions. Does the 2.5s `cameraDuration` parity live inside `PilgrimMap` (out of audited slice)? If not, the cinematic feel is degraded.
- Power-user perf for milestone `detectMilestoneFor` — iOS uses pre-computed `Walk.distance` field; Android does N+1 `repository.locationSamplesFor()` re-haversine on every past walk. Confirm whether Android `WalkSummary` Room entity has a `distanceMeters` denormalized column that should be used instead.
- Per-walk-once seal-reveal store on Android (`SealRevealStore`) is documented as USER-REQUESTED divergence from iOS replay behavior. Confirm this is still the desired UX or whether iOS parity (replay every entry) should be restored.
- Etegami feature on Android (`shareEtegami` / `saveEtegamiToGallery` / `EtegamiBitmapRenderer` / `EtegamiPngWriter` / `EtegamiGallerySaver`) has no iOS equivalent in the audited slice. Confirm whether iOS has a peer feature OR Etegami is Android-only.
- `Dispatchers.Default` vs `Dispatchers.IO` for etegami file writes (`WalkSummaryViewModel.kt:1452`) — Stage 5-C memory says IO is canonical. Should this be changed?
- `runCatching` swallows `CancellationException` in 4 call sites (`WalkSummaryViewModel.kt:651-662, 686-699, 754-766, 1054-1069`). Most are in `persistenceScope` (process-lifetime) where CE is rare, but the family pattern risks re-use in viewModelScope contexts. Consider replacing with explicit `try { ... } catch (ce: CancellationException) { throw ce } catch (t: Throwable) { ... }`.
- Reduce-motion detection on Android compares `ANIMATOR_DURATION_SCALE == 0f` literally (`WalkSummaryScreen.kt:160-166`); iOS uses `@Environment(\.accessibilityReduceMotion)` (binary). A future Android system setting at e.g. 0.1f would silently leave full animations on — should this be `< 0.5f` or use `AccessibilityManager.isEnabled` with `getRecommendedTimeoutMillis`?

---

> Drift report written. Per-lens findings above. Use `/ios-parity port <slice>` to regenerate the parity spec for any drift-critical or missing items.
