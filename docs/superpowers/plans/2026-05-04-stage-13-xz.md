# Stage 13-XZ Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** Ship full-parity AI Prompts button (section 17) + per-section reveal stagger on Walk Summary.

**Architecture:** New `core/prompt/` package with 6 voices + assembler + formatter + photo analyzer + narrative arc + geocoder + 3 sheets/dialogs. Single `PromptsCoordinator` facade injected into VM. Stagger via `rememberRevealAlpha` Composable replacing single `AnimatedVisibility` wrapper.

**Spec:** `docs/superpowers/specs/2026-05-04-stage-13-xz-design.md`.

---

## Setup

```bash
git checkout -b feat/stage-13-xz
```

---

## Task 1: PromptStyle enum + 6 voices + WalkPromptVoice interface

**Files create:**
- `app/src/main/java/.../core/prompt/PromptStyle.kt`
- `app/src/main/java/.../core/prompt/WalkPromptVoice.kt`
- `app/src/main/java/.../core/prompt/voices/{Contemplative,Reflective,Creative,Gratitude,Philosophical,Journaling}Voice.kt`
- Test: `app/src/test/java/.../core/prompt/voices/VoiceStringsTest.kt`

- [ ] Step 1: PromptStyle enum

```kotlin
enum class PromptStyle(val titleRes: Int, val descRes: Int, val icon: ImageVector) {
    Contemplative(R.string.prompt_style_contemplative_title, R.string.prompt_style_contemplative_desc, Icons.Outlined.Spa),
    Reflective(..., Icons.Outlined.Visibility),
    Creative(..., Icons.Outlined.Brush),
    Gratitude(..., Icons.Outlined.Favorite),
    Philosophical(..., Icons.AutoMirrored.Outlined.MenuBook),
    Journaling(..., Icons.Outlined.Edit),
}
```

- [ ] Step 2: WalkPromptVoice interface
```kotlin
interface WalkPromptVoice {
    fun preamble(hasSpeech: Boolean): String
    fun instruction(hasSpeech: Boolean): String
}
```

- [ ] Step 3: 6 voice objects, each with 4 verbatim strings per spec audit Gap 3

- [ ] Step 4: Tests — 24 verbatim string assertions (6 styles × 2 fields × 2 hasSpeech)

```bash
./gradlew :app:testDebugUnitTest --tests "*VoiceStringsTest"
git add app/src/main/java/org/walktalkmeditate/pilgrim/core/prompt/ app/src/test/java/org/walktalkmeditate/pilgrim/core/prompt/voices/
git commit -m "feat(prompt): PromptStyle enum + 6 voice objects (Stage 13-XZ task 1)"
```

---

## Task 2: CustomPromptStyle + CustomPromptStyleStore

**Files create:**
- `core/prompt/CustomPromptStyle.kt` — `@Serializable data class CustomPromptStyle(id: String, title: String, icon: String, instruction: String)`
- `core/prompt/CustomPromptStyleStore.kt` — DataStore Preferences wrapper, key `"custom_prompt_styles"`, JSON via kotlinx-serialization, max=3
- `core/prompt/voices/CustomPromptStyleVoice.kt` — wraps CustomPromptStyle as WalkPromptVoice with iOS-fixed-preamble (verbatim per Gap 3)
- Test: `core/prompt/CustomPromptStyleStoreTest.kt` — max=3 cap silently drops 4th, JSON round-trip, persist across instances, default empty

```bash
git commit -m "feat(prompt): CustomPromptStyle + CustomPromptStyleStore (DataStore JSON, max=3) (Stage 13-XZ task 2)"
```

---

## Task 3: PhotoContext + PhotoContextEntry + PhotoContextAnalyzer

**Files create:**
- `core/prompt/PhotoContext.kt` — `@Serializable data class PhotoContext(tags: List<String>, detectedText: List<String>, people: Int, outdoor: Boolean, dominantColor: String)` (NO animals, NO salientRegion)
- `core/prompt/PhotoContextEntry.kt` — `data class PhotoContextEntry(index: Int, distanceIntoWalkMeters: Double, time: Long, coordinate: LatLng?, context: PhotoContext)`
- `core/prompt/PhotoContextAnalyzer.kt` — DataStore-cached ML Kit pipeline:
  - Cache key `photo_context_${uri.replace("/", "_")}`
  - ML Kit Image Labeling (confidence > 0.3, top 8) → tags
  - ML Kit Text Recognition → detectedText (filter regex: phone/email/url, len ≤ 50, top 5)
  - ML Kit Face Detection → people count
  - Outdoor proxy: tags ∩ `["outdoor", "nature", "sky", "landscape", "field", "mountain", "forest", "park", "beach", "ocean"]` non-empty
  - dominantColor: scale Bitmap to 1×1 via `Bitmap.createScaledBitmap`, read pixel, format `"#%06X".format(0xFFFFFF and color.toInt())`
- Tests: cache hit/miss, label filtering, regex personal-info filter, outdoor proxy, dominantColor pixel-average

```bash
git commit -m "feat(prompt): PhotoContext + Analyzer (ML Kit Image Labeling + Text Recognition + Face Detection + Bitmap pixel-average; cached) (Stage 13-XZ task 3)"
```

---

## Task 4: PhotoNarrativeArc + Builder (no-op stubs)

**Files create:**
- `core/prompt/NarrativeArc.kt` — `data class NarrativeArc(attentionArc: String, solitude: String, recurringTheme: List<String>, dominantColors: List<String>)`
- `core/prompt/PhotoNarrativeArcBuilder.kt` — Per spec § Non-goals: ship no-op stubs. `build(entries: List<PhotoContextEntry>) = NarrativeArc("none", "none", emptyList(), emptyList())`. KDoc cites the salientRegion divergence reason.
- Test: builder returns sentinel-empty NarrativeArc regardless of inputs

```bash
git commit -m "feat(prompt): PhotoNarrativeArc no-op stubs (no ML Kit saliency; salientRegion would mislead LLM) (Stage 13-XZ task 4)"
```

---

## Task 5: PlaceContext + WaypointContext + RecordingContext + MeditationContext + WalkSnippet + ActivityContext + GeneratedPrompt + RouteSampleProjection

Pure data classes + helpers. Single commit, no tests except `RouteSampleProjection` (closestCoordinate + distanceAtTimestamp helpers — verbatim port of iOS `PromptListView.swift:222,272`).

**Files create:**
- `core/prompt/PlaceContext.kt` + `enum class PlaceRole { Start, End }`
- `core/prompt/WaypointContext.kt` (label: String, icon: String?, timestamp: Long, coordinate: LatLng)
- `core/prompt/RecordingContext.kt` (uuid: String, timestamp: Long, startCoordinate: LatLng?, endCoordinate: LatLng?, wordsPerMinute: Double?, text: String)
- `core/prompt/MeditationContext.kt` (startDate: Long, endDate: Long, durationSeconds: Long)
- `core/prompt/WalkSnippet.kt` (date: Long, placeName: String?, weatherCondition: String?, celestialSummary: String?, transcriptionPreview: String)
- `core/prompt/ActivityContext.kt` (all 14 fields per Gap 5 in audit)
- `core/prompt/GeneratedPrompt.kt` (style: PromptStyle?, customStyle: CustomPromptStyle?, title: String, subtitle: String, text: String, icon: ImageVector)
- `core/prompt/RouteSampleProjection.kt` — `closestCoordinate(samples, atMs): LatLng?` + `distanceAtTimestamp(samples, atMs): Double` (cumulative haversine to closest sample) + `String.truncatedAtWordBoundary(maxLength: Int)`
- Test: RouteSampleProjectionTest (3 cases each)

```bash
git commit -m "feat(prompt): context data types + RouteSampleProjection helpers (Stage 13-XZ task 5)"
```

---

## Task 6: PromptGeocoder

**Files create:**
- `core/prompt/PromptGeocoder.kt` — `Geocoder` wrapper, `@Suppress("DEPRECATION")` on sync `getFromLocation(lat, lon, 1)`. `geocodeStart(LatLng)` always; `geocodeEnd(LatLng, distanceFromStartMeters)` only if > 500m. 1.1s `delay()` before second call. Format: `"${address.featureName ?: ""}, ${address.locality ?: ""}".trim().trim(',').takeIf { it.isNotBlank() }`. All on `Dispatchers.IO`. Failure → return null silently.
- Test: 500m gate (start always, end only if > 500m), 1.1s delay assertion (advanceTimeBy under runTest), null on failure

```bash
git commit -m "feat(prompt): PromptGeocoder (500m gate + 1.1s rate-limit delay) (Stage 13-XZ task 6)"
```

---

## Task 7: ContextFormatter

**Files create:**
- `core/prompt/ContextFormatter.kt` — verbatim port of every iOS public format fn per Gap 1 in audit:
  - `formatRecordings(recordings)` — `[time] [GPS: lat, lon → lat, lon] [~N wpm, label] text` joined `"\n\n"`
  - `formatPlaceNames(places)` — Started near X → ended near Y / Near X / nil
  - `formatMeditations(meditations)` — `[time1 – time2] Meditated for N min N sec`
  - `formatPaceContext(speeds)` — filter ≥ 0.3, need ≥ 10, `**Pace:** Average X (range: fast–slow)`
  - `formatPace(metersPerSecond)` — `min:sec min/km|mi`. Use `unitsPreferences.distanceUnits` not Locale (Stage 13-Cel pattern)
  - `formatRecentWalks(snippets)` — `**Recent Walk Context (for continuity):**\n\n[date – place in weather, celestial] "preview"` joined `\n\n`
  - `formatWeather(walk)` — `Weather: condition, temp, humidity N%, windDesc` (conditional parts)
  - `formatMetadata(duration, distance, startDate, lunarPhase)` — `Walk duration: N minutes | Distance: X | Time: timeOfDay on dateTime | Moon: name (N% illumination)`
  - `timeOfDayDescription(date)` — 6 hour-range buckets per audit
  - `speakingPaceLabel(wpm)` — slow|measured|conversational|rapid bucket
  - `formatCelestial(snapshot)` — verbatim per Stage 13-Cel `ContextFormatter.formatCelestial` if already ported, else fresh per audit Gap 1 final entry
  - `formatCoord(lat, lon)` — `"%.5f, %.5f"`
- Test: each fn against iOS-equivalent fixture inputs

```bash
git commit -m "feat(prompt): ContextFormatter — verbatim iOS port of all format fns (Stage 13-XZ task 7)"
```

---

## Task 8: PromptAssembler

**Files create:**
- `core/prompt/PromptAssembler.kt` — `object PromptAssembler { fun assemble(context: ActivityContext, voice: WalkPromptVoice): String }`. Verbatim template per Gap 2 in audit. Section gates per audit. **Drop `Animals:` line** (Android divergence). **Drop `Focal area:` line** + **drop "Visual narrative" + "Color progression" arc block** per Task 4 stub decision. Per-photo `Scene:` / `Text found:` / `People:` / `Outdoor:` / `dominantColor` retained.
- Test: full template all-gates-on; full template all-gates-off; per-gate ON/OFF (~12 tests); intention appended to instruction; voice preamble + instruction wrapping

```bash
git commit -m "feat(prompt): PromptAssembler — verbatim iOS template (Animals + Focal area + arc block dropped; documented divergence) (Stage 13-XZ task 8)"
```

---

## Task 9: PromptGenerator

**Files create:**
- `core/prompt/PromptGenerator.kt` — `object PromptGenerator { fun generateAll(context: ActivityContext): List<GeneratedPrompt> = PromptStyle.entries.map { generate(it, context) } }`. `generate(style, context)` picks the matching voice, calls assembler, builds `GeneratedPrompt`.
- Test: `generateAll` returns 6 prompts in PromptStyle.entries order, each with style + correct icon + non-empty text

```bash
git commit -m "feat(prompt): PromptGenerator — generateAll(context) returns 6 GeneratedPrompts (Stage 13-XZ task 9)"
```

---

## Task 10: PromptsCoordinator + Hilt module

**Files create:**
- `core/prompt/PromptsCoordinator.kt` — facade aggregating CustomPromptStyleStore + PhotoContextAnalyzer + PromptGeocoder. Methods: `suspend fun buildContext(walkId: Long): ActivityContext` (orchestrates all sub-fetches; uses `WalkRepository` + `recentFinishedWalksBefore` + `voiceRecordingsFor` + `waypointsFor` + `photosFor` + `activityIntervalsFor` + `locationSamplesFor`), `customStyles: StateFlow<List<CustomPromptStyle>>`, `suspend fun saveCustomStyle(s)`, `suspend fun deleteCustomStyle(s)`, `fun customVoiceFor(s): WalkPromptVoice`
- `di/PromptModule.kt` — Hilt module providing CustomPromptStyleStore (DataStore), PhotoContextAnalyzer, PromptGeocoder, PromptsCoordinator. Reuse `provideJson` from NetworkModule (Stage 5-C precedent).
- Test: PromptsCoordinator with fake repository + fake stores → buildContext returns ActivityContext with all 14 fields populated correctly (or sentinel-nil per source data)

```bash
git commit -m "feat(prompt): PromptsCoordinator facade + Hilt PromptModule (Stage 13-XZ task 10)"
```

---

## Task 11: Strings.xml

```xml
<!-- Stage 13-XZ AI Prompts -->
<string name="prompts_button_title">Generate AI Prompts</string>
<string name="prompts_button_subtitle_no_speech">Reflect on your walk</string>
<plurals name="prompts_button_subtitle_with_speech">
    <item quantity="one">%d transcription available</item>
    <item quantity="other">%d transcriptions available</item>
</plurals>
<string name="prompts_sheet_title">AI Prompts</string>

<!-- 6 style titles + 6 descriptions -->
<string name="prompt_style_contemplative_title">Contemplative</string>
<string name="prompt_style_contemplative_desc">Sit with what emerged from movement</string>
<!-- ... 5 more (per spec strings section) -->

<!-- Detail -->
<string name="prompt_detail_copy">Copy</string>
<string name="prompt_detail_copied">Copied!</string>
<string name="prompt_detail_share">Share</string>
<string name="prompt_detail_paste_in_ai">Paste in your favorite AI</string>
<string name="prompt_detail_pill_chatgpt">ChatGPT</string>
<string name="prompt_detail_pill_claude">Claude</string>

<!-- Custom editor -->
<string name="custom_prompt_create_title">Create Your Own</string>
<string name="custom_prompt_edit_title">Edit Custom Prompt</string>
<string name="custom_prompt_title_label">Title</string>
<string name="custom_prompt_title_placeholder">e.g., Letter to Future Self</string>
<string name="custom_prompt_icon_label">Icon</string>
<string name="custom_prompt_instruction_label">Instruction</string>
<string name="custom_prompt_instruction_placeholder">Describe how the AI should respond to your walking thoughts…</string>
<string name="custom_prompt_save">Save</string>
<string name="custom_prompt_delete">Delete</string>
<string name="custom_prompt_edit_action">Edit</string>
<plurals name="custom_prompt_counter">
    <item quantity="one">%d of 3 custom styles</item>
    <item quantity="other">%d of 3 custom styles</item>
</plurals>
```

```bash
./gradlew :app:lintDebug
git commit -m "feat(prompt): Stage 13-XZ strings (~25 + 2 plurals) (Stage 13-XZ task 11)"
```

---

## Task 12: WalkSummaryViewModel — promptsSheetState + buildActivityContext

**Files modify:**
- `ui/walk/WalkSummaryViewModel.kt` — inject `PromptsCoordinator`. Add `_promptsSheetState: MutableStateFlow<PromptsSheetState>` + state machine sealed class (Closed / Listing(context) / Detail(listing, prompt) / Editor(listing, editing)). Cache `ActivityContext` in `MutableStateFlow<ActivityContext?>`. Invalidate via `combine(observePhotosFor.map{size}.distinctUntilChanged(), observeVoiceRecordings.map{count{transcription!=null}}.distinctUntilChanged())`. Actions: `openPromptsSheet()`, `closePromptsSheet()`, `openPromptDetail(prompt)`, `openCustomEditor(editing)`, `saveCustomPrompt(s)`, `deleteCustomPrompt(s)`.
- Test: openPromptsSheet → buildContext runs once → cache hit on reopen → invalidation on photo pin/unpin AND on transcription landing

```bash
git commit -m "feat(walk-summary): VM promptsSheetState + ActivityContext cache + invalidation (Stage 13-XZ task 12)"
```

---

## Task 13: AIPromptsRow composable

**Files create:**
- `ui/walk/summary/AIPromptsRow.kt` — `Row` with Material `Icons.Outlined.FormatQuote` + Column(title + subtitle) + Spacer + Icons.AutoMirrored.Outlined.NavigateNext. Subtitle: `pluralStringResource(prompts_button_subtitle_with_speech, recordingsCount, recordingsCount)` if recordings non-empty, else `stringResource(prompts_button_subtitle_no_speech)`. Whole row clickable, parchmentSecondary background, normal corner.
- Test: renders title + dynamic subtitle (no-speech / 1 / 2) + icon + tap fires callback

```bash
git commit -m "feat(walk-summary): AIPromptsRow composable (Stage 13-XZ task 13)"
```

---

## Task 14: PromptListSheet (ModalBottomSheet)

**Files create:**
- `ui/walk/summary/PromptListSheet.kt` — `@OptIn(ExperimentalMaterial3Api::class) ModalBottomSheet` with `LazyColumn` of: 6 `PromptStyleRow`s (built-in) → divider → N `PromptStyleRow`s (custom, with swipe-to-edit + swipe-to-delete) → "Create Your Own" row (disabled when count >= 3). Top bar: "AI Prompts" title.
- Sub-composable `PromptStyleRow(icon, title, subtitle, onClick)` — Row with icon (40dp width) + Column(title + 2-line ellipsized subtitle) + chevron.
- Test: renders 6 built-in + N custom + "Create Your Own"; "Create Your Own" disabled at cap=3; tap fires correct callback per row; swipe-edit fires editor callback; swipe-delete fires delete callback

```bash
git commit -m "feat(walk-summary): PromptListSheet (6 built-in + N custom + Create Your Own) (Stage 13-XZ task 14)"
```

---

## Task 15: PromptDetailDialog

**Files create:**
- `ui/walk/summary/PromptDetailDialog.kt` — `Dialog(onDismissRequest)` with full-screen content: top bar (close) + ScrollableColumn(icon + title HStack + selectable Text(prompt.text)) + Divider + Row(Copy + Share buttons) + AnimatedVisibility(showAIPills, slideIntoVerticallyFromBottom + fadeIn) wrapping Column("Paste in your favorite AI" + Row(ChatGPT pill + Claude pill)).
- Copy: `LocalClipboardManager.current.setText(AnnotatedString(prompt.text))`. Trigger animateScale 1f → 0.95f → 1f. Show "Copied!" feedback. Show pills via state. Cancel + reschedule 8s timer per re-tap. After 8s reset feedback + hide pills.
- Share: `Intent(ACTION_SEND).putExtra(EXTRA_TEXT, prompt.text).type = "text/plain"` via Intent.createChooser.
- Pills: tap → `Intent(ACTION_VIEW, Uri.parse(URL))` for `"https://chat.openai.com/"` / `"https://claude.ai/new"`.
- Test: tap Copy → clipboard set + pills visible + "Copied!" shown; 8s later reset; re-tap resets timer; tap pill → ACTION_VIEW intent dispatched

```bash
git commit -m "feat(walk-summary): PromptDetailDialog (Copy + Share + AI pills + 8s auto-revert) (Stage 13-XZ task 15)"
```

---

## Task 16: CustomPromptEditorDialog

**Files create:**
- `ui/walk/summary/CustomPromptEditorDialog.kt` — Dialog full-screen. Title field (TextField placeholder) + 5-column LazyVGrid(20 Material icons per spec mapping table) + Instruction TextField (min 120dp, multiline) + counter Text (pluralized) + Save button (disabled until title + instruction non-blank).
- Spec lists 20 icons in Material substitution table — all exposed via single `customPromptIcons: List<ImageVector>` constant.
- Test: 20 icons render; selecting icon updates state; Save delegates to `viewModel.saveCustomPrompt`; "Create Your Own" cap-3 disabled state propagated from parent

```bash
git commit -m "feat(walk-summary): CustomPromptEditorDialog (20-icon Material grid + max-3 cap) (Stage 13-XZ task 16)"
```

---

## Task 17: WalkSummaryRevealAnimations + rememberRevealAlpha

**Files create:**
- `ui/walk/summary/WalkSummaryRevealAnimations.kt`:

```kotlin
internal const val REVEAL_DURATION_HERO_MS = 600
internal const val REVEAL_DURATION_QUOTE_MS = 800
internal const val REVEAL_DURATION_CALLOUT_MS = 800
internal const val REVEAL_DURATION_DEFAULT_MS = 600
internal const val REVEAL_DELAY_STATS_MS = 200
internal const val REVEAL_DELAY_CELESTIAL_MS = 300
internal const val REVEAL_DELAY_BREAKDOWN_MS = 400

@Composable
internal fun rememberRevealAlpha(
    revealPhase: RevealPhase,
    durationMs: Int,
    delayMs: Int,
    reduceMotion: Boolean,
    fireOnZoomed: Boolean = false,
): Float {
    val target = when {
        fireOnZoomed -> if (revealPhase != RevealPhase.Hidden) 1f else 0f
        else -> if (revealPhase == RevealPhase.Revealed) 1f else 0f
    }
    val anim by animateFloatAsState(
        targetValue = target,
        animationSpec = if (reduceMotion) tween(0)
                        else tween(durationMs, delayMillis = delayMs, easing = EaseIn),
        label = "reveal-alpha-$durationMs-$delayMs",
    )
    return anim
}
```

- Test: rememberRevealAlpha targets per phase + fireOnZoomed; reduceMotion collapses to 0ms

```bash
git commit -m "feat(walk-summary): WalkSummaryRevealAnimations + rememberRevealAlpha (Stage 13-XZ task 17)"
```

---

## Task 18: WalkSummaryScreen wiring

**Files modify:**
- `ui/walk/WalkSummaryScreen.kt`:
  - Replace section-17 placeholder comment with `AIPromptsRow(recordingsCount = recordings.size, onClick = viewModel::openPromptsSheet)`.
  - Render `PromptListSheet` / `PromptDetailDialog` / `CustomPromptEditorDialog` based on `viewModel.promptsSheetState.collectAsStateWithLifecycle()`.
  - **Remove `AnimatedVisibility(...)` wrapper** around sections 4-11 (currently lines ~310-413). Replace with plain `Column(verticalArrangement = spacedBy(normal))` containing sections 4-11 each with `Modifier.alpha(rememberRevealAlpha(revealPhase, ..., reduceMotion = reduceMotion, fireOnZoomed = ...))`.
  - **Move sections 12-15** (FaviconSelectorCard / WalkActivityTimelineCard / WalkActivityInsightsCard / WalkActivityListCard) OUTSIDE the (now-removed) wrapper into the parent Column — they render immediately, no fade. Match iOS.
  - durationHero special: `fireOnZoomed = true`.
  - ElevationProfile + sections without iOS reveal opacity → no alpha modifier.

Per-section table (apply within new Column):
| Section | duration | delay | fireOnZoomed |
|---|---|---|---|
| ElevationProfile | (no alpha) | — | — |
| WalkJourneyQuote | QUOTE | 0 | false |
| WalkDurationHero | HERO | 0 | true |
| MilestoneCalloutRow | CALLOUT | CELESTIAL_MS (300) | false |
| WalkStatsRow | DEFAULT | STATS_MS (200) | false |
| WalkSummaryWeatherLine | DEFAULT | STATS_MS (200) | false |
| CelestialLineRow | DEFAULT | CELESTIAL_MS (300) | false |
| WalkTimeBreakdownGrid | DEFAULT | BREAKDOWN_MS (400) | false |

```bash
./gradlew :app:assembleDebug
git commit -m "feat(walk-summary): wire AIPromptsRow + sheets + per-section reveal stagger (Stage 13-XZ task 18)"
```

---

## Task 19: Final integration

```bash
./gradlew :app:assembleDebug :app:lintDebug :app:testDebugUnitTest
```

Manual on-device:
- Open finished walk → tap "Generate AI Prompts" → list renders within ~3s on first open
- Reopen → instant (cached)
- Tap Contemplative → detail shows full prompt text
- Tap Copy → "Copied!" + pills slide up
- Tap ChatGPT → opens browser
- 8s later: pills slide down, label resets
- Re-tap Copy → resets timer
- Tap Share → Android chooser
- Create custom prompt → 20-icon grid → Save → returns to list
- Add 3 → "Create Your Own" disabled
- Swipe-edit / swipe-delete custom prompt
- Toggle airplane mode → reopen → location section omitted (geocoder fails silently)
- Pin/unpin photo → reopen sheet → photo block updates (cache invalidated)
- Auto-transcribe lands → reopen sheet → transcription block updates (cache invalidated)
- Walk Summary reveal: visually verify staggered fade across 7 sections, durationHero appears with map-zoom, timeBreakdown last at 400ms

## Self-review

- [ ] Spec coverage: every section of `2026-05-04-stage-13-xz-design.md` has a task
- [ ] No placeholders in plan tasks
- [ ] Type consistency: `transcription` (not `transcript`), PromptsSheetState `Listing` (not `List`), animals + salientRegion fields ABSENT from PhotoContext
- [ ] Task 17 (rememberRevealAlpha) precedes Task 18 (screen wiring)
- [ ] All commits compile + tests pass at each step
